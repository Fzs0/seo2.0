{
  "name": "exdivo-social-publisher-executor",
  "version": "1.0.0",
  "private": true,
  "description": "Cross-platform local Hubstudio publishing executor",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "test": "node --test tests/*.test.js"
  },
  "engines": { "node": ">=20" },
  "dependencies": { "puppeteer-core": "^24.15.0" }
}
