{
  "name": "seo-social-executor",
  "version": "0.1.0",
  "private": true,
  "description": "Local-only Hubstudio assisted publishing executor",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "test": "node --test tests/*.test.js"
  },
  "engines": { "node": ">=20" },
  "dependencies": { "puppeteer-core": "^24.15.0" }
}
