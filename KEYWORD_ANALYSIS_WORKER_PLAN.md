# 关键词 AI 分析独立 Worker 方案

状态：未来扩容方案，当前不实施

更新时间：2026-07-15（Asia/Shanghai）

## 1. 为什么需要这个方案

当前关键词 AI 分析运行在 FastAPI 进程内。页面创建任务后，后台协程负责持续调用 AI、写回关键词结果和更新进度。

这个方式在关键词较少时足够简单，但关键词量变大后会出现几个业务问题：

- 后端重启会中断分析；
- 一次 AI 请求失败可能影响整个分析任务；
- 任务进度只记录在总任务上，无法准确知道每个关键词是否成功；
- 不能稳定支持夜间长时间运行、自动重试和多批次并发；
- 任务范围、站点分配清理和执行队列取消容易互相影响。

当前代码还存在参数传递风险：任务 `payload` 保存了 `keyword_ids` 和 `opportunity_type`，但进程内后台任务没有从 `payload` 恢复参数。未来 Worker 必须把数据库任务记录作为唯一事实来源。

## 2. 结合实际业务的目标场景

例如一次导入了 1224 个关键词：

1. AI 分析完成 40 个，剩余 1184 个；
2. 运营人员希望先分析其中 30 个长尾词；
3. 系统按 10 个一批调用 AI；
4. AI 请求失败或服务器重启；
5. 第二天系统继续处理未完成的关键词，而不是重新分析全部关键词；
6. 每个关键词分析完成后，才允许分配站点并进入策略生成。

最终需要看到的是：

```text
总任务：1224
已成功：40
处理中：10
待处理：1171
失败待重试：3
已取消：0
```

而不是只看到一个无法解释的“任务仍在 running”。

## 3. 目标与非目标

### 目标

- 分析任务独立于 FastAPI 进程；
- 固定本次任务的关键词范围和筛选条件；
- 每个关键词有独立状态、重试次数和错误信息；
- Worker 重启后可以继续未完成项；
- 同一个关键词不会因为重试重复产生错误策略；
- 只有分析成功并通过现有业务校验的关键词才能分配站点；
- 清理分配和取消队列只影响当前分析任务的目标范围；
- 页面可以展示可信的总进度和阶段日志。

### 非目标

- 不在本方案中实现 AI 策略大脑；
- 不改变现有关键词相关性、市场、语种和站点校验规则；
- 不自动抓取外部网页；
- 不因为引入 Worker 而增加 pgvector 或新的知识库系统；
- 不改变“人工审核后才能进入执行队列”的业务门禁。

## 4. 建议架构

```text
React 页面
    │
    ▼
FastAPI：创建分析任务、查询状态、取消任务
    │
    ├── 任务记录：seo_agent.tasks
    ├── 分析批次：keyword_analysis_runs
    └── 关键词明细：keyword_analysis_items
            │
            ▼
独立 Keyword Analysis Worker
    │
    ├── 领取待处理关键词
    ├── 每批最多提交 10 个给 AI
    ├── 写回 ai_review、站点分配和明细状态
    └── 更新任务汇总进度
```

优先使用 PostgreSQL 作为任务状态的事实来源。现有 Redis 可以用于唤醒 Worker，但不能只把任务状态放在 Redis 或进程内存中。

## 5. 数据模型建议

### 5.1 `keyword_analysis_runs`

一行代表一次“分析 30 个关键词”或“分析本次导入剩余关键词”的业务任务。

建议字段：

- `id`：分析任务 UUID；
- `task_id`：关联现有 `seo_agent.tasks`；
- `status`：`queued`、`running`、`done`、`failed`、`canceled`；
- `opportunity_type`：`long_tail`、`qa`、`low_kd` 或空；
- `total_count`、`success_count`、`failed_count`、`canceled_count`；
- `max_attempts`；
- `created_at`、`started_at`、`finished_at`、`updated_at`；
- `error_summary`。

### 5.2 `keyword_analysis_items`

一行代表本次任务中的一个关键词。

建议字段：

- `run_id`；
- `keyword_id`；
- `status`：`pending`、`running`、`done`、`retryable`、`failed`、`canceled`；
- `attempts`；
- `lease_until`：Worker 异常退出后用于回收；
- `last_error`；
- `started_at`、`finished_at`、`updated_at`；
- `UNIQUE (run_id, keyword_id)`。

创建任务时就写入目标关键词快照。这样“分析剩余关键词”也有明确范围，不会因为任务运行期间又导入新关键词而改变总数。

## 6. 任务执行流程

### 6.1 创建任务

API 收到请求后：

1. 校验 `keyword_ids` 和 `opportunity_type`；
2. 根据筛选条件查询目标关键词；
3. 将目标关键词 ID 写入 `keyword_analysis_items`；
4. 写入任务总数和筛选条件；
5. 事务提交后通知 Worker；
6. 返回 `run_id`。

如果目标集合为空，应直接返回“没有待分析关键词”，不要创建一个会长时间 running 的空任务。

### 6.2 Worker 领取任务

Worker 使用数据库事务领取待处理项，并设置 `lease_until`。建议使用行锁和 `SKIP LOCKED`，避免多个 Worker 重复领取同一关键词。

初期只启动一个 Worker，确认稳定后再提高并发。AI 请求仍按现有逻辑每批最多 10 个关键词，避免一次请求过大。

### 6.3 成功处理

AI 返回后：

1. 校验返回 JSON 和关键词 ID；
2. 对每个关键词执行现有相关性、市场、语种和站点校验；
3. 成功写入 `ai_review`；
4. 只有符合现有规则的结果才写入站点分配；
5. 将对应明细标记为 `done`；
6. 更新任务汇总进度。

这些写入应放在同一个数据库事务中，避免出现“明细显示完成，但关键词没有结果”的状态。

### 6.4 失败与重试

可重试错误包括：

- AI 供应商超时；
- 网络暂时不可用；
- 供应商返回 429 或 5xx；
- 数据库短暂连接失败。

不可重试错误包括：

- AI 返回无法解析的内容；
- 关键词数据本身无效；
- 业务校验明确拒绝。

重试应增加 `attempts`，并使用退避时间。超过 `max_attempts` 后标记为 `failed`，但不能阻塞其他关键词继续处理。

### 6.5 Worker 重启

Worker 启动时：

1. 将 `lease_until` 已过期的 `running` 项恢复为 `retryable`；
2. 继续领取 `pending` 和 `retryable` 项；
3. 已经是 `done` 的关键词不重复处理；
4. 任务汇总根据明细重新计算。

## 7. “独立放大风险”是什么

当前 `start_keyword_analysis()` 在启动任何 AI 分析前，会调用 `clear_unreviewed_assignments()`。保存 CSV 导入也会自动启动这条流程，因此风险不只发生在页面点击“开始 AI 分析”时。

这个清理函数不是只清理本次选中的关键词，而是会处理所有“尚未完成 AI 分析”的关键词，包括：

- 清空已有站点分配；
- 把 `analyzed`、`planned`、`hold` 状态重置为 `imported`；
- 取消这些关键词关联的排队任务。

这本身就是一个独立风险。它和参数丢失不是同一个问题，但参数丢失会把它放大。

### 具体业务场景

假设 1224 个关键词中：

- 30 个是运营人员刚选中的长尾词；
- 100 个关键词之前已经完成部分处理并分配了站点；
- 8 个关键词已经通过审核，正排队等待生成文章；
- 其余关键词还没有开始 AI 分析。

运营人员本来只想重新分析那 30 个长尾词，但由于参数丢失，系统实际启动了“所有未分析关键词”的任务。类似地，保存一个新的关键词文件，也可能在导入动作完成后触发同样的全局清理。

启动前的全局清理可能导致：

1. 原来已经分配好的 100 个关键词失去站点；
2. 其中部分关键词回到 `imported`，需要重新走分析流程；
3. 8 个已经审核并排队的文章任务被取消；
4. 后续策略页面看到的数量、站点和优先级发生变化；
5. AI 又重新分析大量不在本次业务范围内的关键词，增加成本和等待时间。

因此，一个“只分析 30 个词”的按钮操作，可能影响另外 1194 个关键词。这就是放大风险：输入范围很小，副作用范围却是整个未完成数据集。

### Worker 方案中的控制方式

- 创建任务时固定目标关键词快照；
- 清理操作必须带 `run_id` 或关键词 ID 集合；
- 不允许通过无范围条件清理全部未完成关键词；
- 取消任务时只取消当前任务创建的队列项；
- 执行前记录将被清理和取消的数量；
- 所有清理、取消和重置动作写入任务日志，便于恢复和审计。

如果未来确实需要“全量重置”，应该做成明确的管理员操作，并在页面上显示影响范围，不应作为普通 AI 分析的隐含副作用。

## 8. 分阶段实施建议

### 阶段一：补数据结构

- 增加 `keyword_analysis_runs`；
- 增加 `keyword_analysis_items`；
- 创建任务时固定关键词快照；
- 保留现有 API 返回结构，减少前端改动。

### 阶段二：实现独立 Worker

- 独立启动命令；
- 数据库领取和租约机制；
- 成功、失败、重试和取消；
- 任务汇总状态；
- 运行日志和错误日志。

### 阶段三：小规模试运行

先用少量关键词验证：

- 选定关键词是否严格限定在任务内；
- Worker 重启后是否继续；
- AI 超时是否只重试失败项；
- 一个关键词是否可能重复分配站点；
- 清理操作是否影响任务范围外的数据。

### 阶段四：替换进程内任务

确认 Worker 稳定后，再移除或关闭 FastAPI 进程内的长任务执行逻辑。API 只负责创建、查询和取消任务。

## 9. 验收标准

- 选择 3 个关键词时，只产生 3 条分析明细；
- 选择 `long_tail`、`qa`、`low_kd` 时，实际目标集合符合筛选条件；
- 任务执行期间新增关键词不会改变本次任务总数；
- Worker 重启后能继续未完成项；
- 已完成关键词不会被重复分析；
- 失败关键词可以单独重试；
- 任务取消后不会继续领取新的关键词；
- 任务范围外的站点分配和文章任务不被清理或取消；
- 页面进度与明细统计一致；
- AI 成本和请求数量可按任务追踪；
- 完整测试覆盖参数范围、重启、重试、取消和越界清理。

## 10. 什么时候真正实施

出现以下任意情况时，再启动本方案：

- 单次关键词分析经常运行超过一个开发进程生命周期；
- 需要夜间或定时自动分析；
- 关键词规模明显增长，失败重跑成本变高；
- 运营开始同时维护多个分析任务；
- 需要准确统计每个关键词的失败原因和 AI 成本；
- FastAPI 重启导致任务中断已经影响日常工作。

在此之前，先完成当前的参数透传和任务 `payload` 恢复方案即可。
